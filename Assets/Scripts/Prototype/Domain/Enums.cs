namespace GuildProto
{
    public enum Rank { Bronze, Silver, Gold }
    public enum Job { None, Warrior, Archer, Mage, Priest }
    public enum CountBand { Few, Pack, Horde }
    public enum Result { Great, Success, Injured, Fail }
}
